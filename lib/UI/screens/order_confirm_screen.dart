import 'package:flutter/material.dart';

class ConfiremOrderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('confirm screen'),
    );
  }
}